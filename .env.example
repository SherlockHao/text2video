DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/text2video
REDIS_URL=redis://redis:6379/0
DEBUG=true
ALLOWED_ORIGINS=http://localhost:3000
STORAGE_ROOT=./storage

# TTS (choose one)
TTS_PROVIDER=minimax_tts
MINIMAX_API_KEY=your-minimax-api-key

# ELEVENLABS_API_KEY=your-elevenlabs-key  # alternative TTS

# LLM (Qwen) — configured via vendor/qwen/config.py

# Image/Video Gen (Jimeng) — configured via vendor/jimeng/config.py

# Kling (video, normal quality) — pending
# KLING_API_KEY=
# KLING_BASE_URL=
